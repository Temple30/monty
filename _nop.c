#include "monty.h"

/**
 * customNoOperation - performs no operation.
 * @stackHead: stack head
 * @lineNumber: line_number
 * Return: no return
 */
void customNoOperation(stack_t **stackHead, unsigned int lineNumber)
{
    (void)lineNumber;
    (void)stackHead;
}
