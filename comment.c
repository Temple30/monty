#include "monty.h"

/**
 * comment - Does nothing, treats the line as a comment
 * @stack: Pointer to the stack
 * @line_number: Line number in the file
 */
void comment(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	/* This opcode does nothing, treats the line as a comment */
}
